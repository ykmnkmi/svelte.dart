<script type="text/dart">
  // some external modifier or annotation
  int count = 0;

  $: {
    if (count.isEven) {
      dispatch('even', detail: count);
    } else {
      dispatch('odd', detail: count);
    }
  }
</script>

Clicked { count } { count == 1 ? 'time' : 'times' } <slot name="zero">, click it</slot>!