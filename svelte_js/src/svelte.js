import {
  // operations.dart
  child, child_frag, sibling,
  // render.dart
  template, open, open_frag, close, close_frag, event, text_effect, text, html, attr, mount, append_styles,
  // runtime.dart
  get, set, mutable_source, push, pop, init,
} from 'svelte/internal';

export default {
  child, child_frag, sibling,
  template, open, open_frag, close, close_frag, event, text_effect, text, html, attr, mount, append_styles,
  get, set, mutable_source, push, pop, init,
};
